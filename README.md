# CIFAR_10_Smirnova
ЗИТ-24м. Практика 3. Вариант 9. Для БД CIFAR-10 – полноцветных изображений реальных объектов создать архитектуру сверточной НС с наименьшим числом нейронов, достаточных для правильной классификации изображений тестовой выборки на уровне не менее 92%. Обучить НС с контролем эффекта переобучения путем использования выборки валидации.

Epoch 100/100 | Train Loss: 0.3268 Acc: 0.8848 | Val Loss: 0.3368 Acc: 0.8836 | LR: 0.00000
Обучение завершено за 7121 секунд
Используется устройство: cuda
Используется устройство: cuda
Точность на тестовой выборке: 0.9189
Условие не выполнено. Попробуйте увеличить количество эпох или использовать более сильную аугментацию.

[Графики.docx](https://github.com/user-attachments/files/26413790/default.docx)

Выводы:

1) Разработанная архитектура
• 4 свёрточных блока с фильтрами: 64 → 128 → 256 → 512.
• В каждом блоке: два свёрточных слоя 3×3, BatchNormalization, ReLU, MaxPool2d, Dropout2d(0.3).
• В конце: GlobalAveragePooling2d, полносвязный слой на 10 классов.
• Общее число параметров: 4 694 346 (это выше минимально возможного, но оправдано необходимостью достичь высокой точности).

2) Аугментация и регуляризация
• Аугментация: RandomCrop, RandomHorizontalFlip, ColorJitter (brightness/contrast/saturation = 0.2), Cutout (размер 16).
• Регуляризация: Dropout2d(0.3) в каждом блоке, BatchNormalization, weight decay (5e-4) в оптимизаторе AdamW.
• Оптимизатор: AdamW с начальной скоростью обучения 0.001 и косинусным затуханием с прогревом (warmup 5 эпох).

3) Результаты: На рисунке выше представлены графики изменения точности и функции потерь на тренировочной и валидационной выборках в процессе обучения (100 эпох). Видно, что после 70-й эпохи валидационная точность стабилизируется на уровне около 88%, а тренировочная продолжает медленно расти, что указывает на отсутствие сильного переобучения благодаря применённым методам регуляризации (Dropout, BatchNorm, weight decay). Итоговая точность на тестовой выборке составила 91.89%, что лишь на 0.31% ниже требуемого порога.

___________________________________________________________________________________________________________________________________________________________________________________________________________________________

Дополнительное задание для варианта 9: 
Добавить (или убрать, если они были в исходной сети) слои Dropoutи сравнить качество классификации до и после внесенных изменений. Подобрать параметр слоя Dropout для получения лучшей классификации тестовой выборки.
Объяснить полученные результаты.

=== Сравнение ===
Dropout 0.0: 0.9321
Dropout 0.3: 0.8978
Dropout 0.5: 0.8378

Лучший параметр Dropout: 0.0 с точностью 0.9321
Базовая модель (без Dropout) показала наилучший результат.

